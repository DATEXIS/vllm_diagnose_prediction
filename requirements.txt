# torch must be installed from the CUDA wheel (see Dockerfile); pinning here
# prevents pip from downgrading to the CPU-only wheel when resolving
# sentence-transformers dependencies.
torch>=2.0.0
vllm
openai
aiohttp
tqdm
pandas
pyarrow
pyyaml
scikit-learn
pydantic
wandb
sentence-transformers
jinja2

pytest
pytest-asyncio
