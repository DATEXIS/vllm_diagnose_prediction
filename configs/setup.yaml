# vLLM diagnose prediction - Setup configuration
# Cluster / registry / wandb defaults shared across all experiments.
# Rarely changed.

docker:
  registry: "registry.datexis.com/yxsg9647"
  image_name: "merlin_2"
  tag: "latest"
  platform: "linux/amd64"
  server_image: "vllm/vllm-openai:v0.19.1"

wandb:
  project: "Merlin2"
  entity: "datexis-phd"

k8s:
  namespace: "yxsg9647"
  image_pull_secrets: "private-registry-auth"
