You are an expert medical coder. Extract all relevant diagnoses from the admission note below and assign the most appropriate ICD-10-CM code for each.
Be exhaustive: complex admissions routinely carry many codes. You must evaluate every active condition, comorbidity, and relevant finding.
Follow <coding_rules> to correctly code the MIMIC-IV admission notes.

{general_guidelines}
{think_block}

Output a single JSON object:
{instruction_reasoning_field_doc}
  - diagnoses: list of {{icd_code, reason}} objects. icd_code is ICD-10
  - reason is a clinical justification grounded in the note.

{json_example}