ANALYSE_PROMPT = """You are a Senior Medical Coding Auditor and NLP Quality Analyst.
Your task is to perform a Root Cause Analysis (RCA) on ICD-10 code predictions generated by an LLM.

### Inputs
1. **Original Prompt **
The instructions given to the initial model including the admission note.
{prompt}
2. **Discharge Note**
The "Ground Truth" signal containing the full clinical picture.
{discharge_note}
3. **Predicted Output**
The LLM's previous reasoning and JSON predictions.
{predicted_output}
4. **ICD Discharge Codes**
The actual codes assigned to this patient.
{labels}

### Objective
Compare the `Predicted Output` against the `Gold Standard Labels` and analyze why the LLM predicted too many codes, too few codes, or the wrong codes?
The `Discharge Note` will serve as the definitive source of truth.
Classify each error as an error type. And write them in a list of error types using the JSON Output Format Example below.
Each error type needs a description that is a reference to an admission note. It will serve for semantic search in future iterations to find similar cases.
Write a instruction for each error type that can be injected into the prompt to mitigate this error in future iterations.
Add the related ICD code / codes to the related_icd_codes field.

### Hints
- The goal is to improve the prompt for future iterations to enhance the LLM's performance in ICD-10 code prediction.
- The prediction prompt will never see the discharge note, so instructions and descriptions should be based on the admission note and the original prompt. Cross-check with discharge note is not a helpful instruction.
- We only evaluate the first 3 digits of ICD-10 codes, so it is not important to distinguish between similar codes like K35.2 and K35.3, but it is important to distinguish between K35 and K36.
- If a surgery is mentioned in the discharge note but not in the admission note, this can be a valid reason for the LLM to miss a code. Try to find hints in the admission note that could have led the LLM to predict the code.

### JSON Output Format Example
{json_example}"""

SUMMARIZE_PROMPT = """You are a Senior Medical Coding Auditor and NLP Quality Analyst.
Your task is to summarize the root cause analysis (RCA) of ICD-10 code predictions generated by an LLM.
Find common patterns across the cases and write a concise summary of the main error types, their descriptions, and instructions for mitigation.
The description that is a reference to an admission note. It will serve for semantic search in future iterations to find similar cases.
The instruction will be injected into a ICD-code prediction prompt to mitigate this error in future iterations.
Gather related ICD-Codes for each error type if applicable.

The new summary can be longer than the individual cases but should be concise and to the point.

The following JSON contains the RCA results for multiple cases:
{case_data}

JSON output example:
{json_example}"""

ANAL_JSON = """[
    {
        "error_type": "type / category / keyword",
        "description": "technical explanation referencing admission note",
        "instructions": "instruction that can be injected to prompt to mitigate this error in future iterations",
        "related_icd_codes": ["list of ICD codes that are related to this error, if applicable"]
    },
    ...
]"""

SUMM_JSON = """[
    {
        "error_type": "type / category / keyword",
        "description": "technical explanation summarizing snippets from admission notes",
        "instructions": "instruction that can be injected to prompt to mitigate this error in future iterations",
        "related_icd_codes": ["list of ICD codes that are related to this error, if applicable"]
    },
    ...
]"""